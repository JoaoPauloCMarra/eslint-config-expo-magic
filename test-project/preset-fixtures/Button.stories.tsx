import { Button } from 'react-native';

const storyProbe: any = null;

export default function ButtonStories() {
	console.log('story debug', storyProbe);

	return <Button title="Story" onPress={() => {}} />;
}
